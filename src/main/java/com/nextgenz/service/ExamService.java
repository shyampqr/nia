package com.nextgenz.service;

import org.springframework.stereotype.Service;

@Service
public class ExamService {
    // This service is deprecated and replaced by QuestionService and client-side logic.
    // Keeping file to avoid potential dependency errors until fully cleaned up.
}
